# ─── BarbeariaSuite Platform — Variáveis de Ambiente ─────────────────────────
# Copie este arquivo para .env (dev), .env.staging ou .env.production
# e preencha com os valores reais. NUNCA commitar arquivos .env com secrets.

# ── Database ──────────────────────────────────────────────────────────────────
# MariaDB / MySQL connection string
DATABASE_URL="mysql://barbearia_user:sua_senha_aqui@localhost:3306/barbeariasuite"

# ── NextAuth ──────────────────────────────────────────────────────────────────
# URL base da aplicação (sem barra final)
#   Dev:      http://localhost:3998
#   Staging:  https://suite.franquiabv.xyz
#   Prod:     https://app.seudominio.com.br
NEXTAUTH_URL="http://localhost:3998"

# Secret aleatório — gerar com: openssl rand -base64 32
NEXTAUTH_SECRET="seu_segredo_aleatorio_aqui"

# ── Google OAuth (opcional) ───────────────────────────────────────────────────
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ── WahaSend — WAHA (WhatsApp HTTP API) ──────────────────────────────────────
WAHA_API_URL="http://localhost:3001"
WAHA_API_KEY=""

# ── ERP — franquia_producao (somente leitura) ────────────────────────────────
# Banco de dados legado do sistema de gestão das unidades.
# Recomendado: criar usuário MySQL somente-leitura no servidor do ERP.
ERP_DB_HOST="ip-do-servidor-erp"
ERP_DB_PORT="3306"
ERP_DB_USER="usuario_readonly"
ERP_DB_PASSWORD="senha_aqui"
ERP_DB_NAME="franquia_producao"

# ── VipCam Python Backend (opcional) ─────────────────────────────────────────
# URL do serviço FastAPI de análise facial — configurável também via UI
# VIPCAM_BACKEND_URL="http://localhost:8000"

# ── Churn Prediction (MiroFish) ──────────────────────────────────────────────
# Configuração do microservice de predição de churn
CHURN_API_URL="http://localhost:8000"
CHURN_ENABLED=true

# ── Modo Produção ─────────────────────────────────────────────────────────────
NODE_ENV="production"
